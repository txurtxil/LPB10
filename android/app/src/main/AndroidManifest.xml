<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Deteccion de conduccion via Bluetooth. Confirmado el 04/09/2026 con
         datos reales: sin este permiso concedido en ejecucion, Android 12+
         no entrega el broadcast en absoluto (no es solo que falte el
         nombre/MAC, como se penso al principio: no llega nada). -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- Foreground service que mantiene el sondeo activo mientras conduces. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Obligatorio ademas del FOREGROUND_SERVICE base: con targetSDK 34+ el
         sistema exige el permiso especifico del tipo declarado. Sin el, el
         startForeground lanza SecurityException en caliente aunque el
         manifest declare bien el foregroundServiceType. Confirmado en el log
         del 05/09/2026: "Starting FGS with type specialUse ... requires
         permissions: [android.permission.FOREGROUND_SERVICE_SPECIAL_USE]".
         Se perdio al migrar de dataSync a specialUse: se quito el permiso
         viejo y no se puso el nuevo. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <application
        android:label="LMB10"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <receiver android:name=".BatteryWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/battery_widget_info" />
        </receiver>
        <receiver android:name=".QuickWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_widget_info" />
        </receiver>
        <!-- Sin esto, HomeWidgetBackgroundIntent emite el broadcast y no lo
             recoge nadie: los botones del widget no hacian absolutamente nada
             y el log no registraba ni un intento. Lo declara el propio ejemplo
             del plugin, no se fusiona solo. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>
        <service
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />
        <!-- Arranca/para CarDriveService segun se conecte o desconecte
             cualquier dispositivo Bluetooth. Confirmado el 04/09/2026 que el
             coche expone DOS conexiones (TCU + audio); se dispara con la
             primera de las dos y se para solo cuando ambas se han ido. -->
        <receiver android:name=".CarBtReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
        </receiver>
        <!-- specialUse: mantiene vivo el sondeo (red + escritura en disco)
             de la posicion y estado del coche mientras se conduce. No encaja
             en "dataSync" (esa lista cerrada de usos no cubre esto) ni en
             "location" (el GPS no lo captura este servicio, lo hace la
             llamada normal a la API del vehiculo). El texto del subtype es
             el que se explica en Play Console al declarar el tipo. -->
        <service android:name=".CarDriveService" android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Mantiene activo el sondeo periodico (cada 90s) del estado del vehiculo mientras el usuario conduce, para poder grabar el recorrido GPS completo del trayecto. Se activa solo mientras el movil esta conectado por Bluetooth al coche, y se detiene en cuanto se desconecta." />
        </service>
    </application>
</manifest>
